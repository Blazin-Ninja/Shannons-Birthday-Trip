{
  "$schema": "https://cursor.com/schemas/environment.schema.json",
  "name": "Shannon's Trip · Android SDK 35",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "npm ci && cd android && ./gradlew --no-daemon help"
}
